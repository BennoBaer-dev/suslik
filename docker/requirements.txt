# ALLE Versionen gepinnt (A15, 25.07.): aus den ausgelieferten 0.1.0.23-Images gelesen
# (pip list --format=freeze; alle drei Varianten identisch). Ungepinnt bekam jeder Bau,
# was pip gerade liefert — genau das haette die backend-eigenen Soll-Staende der Abnahme
# entwertet (onnxruntime-Fall vom 25.07.). Heben = bewusste Entscheidung + neue Abnahme.
# suslik Container-Deps (Stufe 1, CPU). Gegenueber der Repo-requirements.txt ergaenzt:
#   PyYAML + paho-mqtt (vom Code gebraucht, fehlten) — Fund aus dem 1. Docker-Test.
#
# WECHSEL 17.09.2026 (0.1.0.541, E4): onnxruntime==1.27.0 -> onnxruntime-openvino==1.24.1,
# also derselbe Stack wie in requirements.gpu.txt. DAS IST GENAU DIE „bewusste
# Entscheidung + neue Abnahme", die der Kopf hier seit dem 25.07. verlangt, und sie ist
# GEMESSEN, nicht vermutet:
#   Feldtester-Vergleich vom 10.09.2026 (Discussion #29, dieselbe Maschine, dieselben
#   Modelldateien, alle Modelle des Urteilspfads): der OpenVINO-CPU-EP rechnet
#   durchgehend 3-7x schneller als das nackte onnxruntime — adaface 48,7 ms je
#   Inferenz statt 192,2 ms.
# Das cpu-Image ist der universelle Rueckfall und damit die Variante, die am wenigsten
# Rechenzeit uebrig hat; ein Faktor 3-7 auf dem Urteilspfad ist dort der groesste
# Einzel-Hebel, den wir haben.
#
# DIE ABNAHME, die dieser Wechsel verlangt (und die er bekommen hat): Vorher/Nachher am
# Grundwahrheits-Korpus, .526-cpu gegen 0.1.0.541-cpu, je Event Wanduhr UND Urteil —
# ein anderer Inferenz-Stack darf schneller sein, aber nicht anders entscheiden.
# Ergebnis siehe CHANGELOG 0.1.0.541.
#
# ZWEI PUNKTE, die aus requirements.gpu.txt WORTGLEICH weitergelten:
#   * insightface zieht plain `onnxruntime` rein und VERDECKT damit die
#     openvino-Variante (kein OV-EP). Das Dockerfile erzwingt danach, dass
#     onnxruntime-openvino gewinnt (uninstall + reinstall --no-deps).
#   * standalone `openvino` NICHT ungepinnt hier hineinnehmen — pip zieht eine neuere
#     Fassung als die, die onnxruntime-openvino 1.24.1 gebuendelt mitbringt. Der Pin
#     steht im Dockerfile (2025.4.1, dieselbe Zahl wie im gpu-Image).
#
# WAS NICHT MITKOMMT (Unterschied zum gpu-Image, ausdruecklich): KEINE
# Intel-GPU-/NPU-Treiberpakete (compute-runtime, IGC, libze, NPU-Tarball). Der
# OpenVINO-CPU-Plugin braucht keinen Treiber — er rechnet auf der CPU. Das cpu-Image
# bleibt damit das treiberfreie, ueberall lauffaehige Image, das es war.
insightface==1.0.1
onnxruntime-openvino==1.24.1
opencv-python==5.0.0.93
scikit-learn==1.9.0
numpy==2.5.1
PyYAML==6.0.3
paho-mqtt==2.1.0
